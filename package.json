{
  "name": "test-stock",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Suivi des positions copiees depuis X : lecture des signaux, risque agrege, actions groupees",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
