{
  "name": "test-stock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Analyse pre-resultats : scraping multi-sources et verdict d'entree sur une action",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
